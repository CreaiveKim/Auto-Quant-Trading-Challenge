# 업데이트 노트

프로젝트 진행 과정을 버전별로 정리함. 포트폴리오에서 읽었을 때 작업 흐름이 보이도록 기술적 내용과 의사결정 이유를 같이 남김.

## v0.1.0 - 프로젝트 초기 구성 (2026-02-24)

커밋: `1c1bebb`, `40eb526`, `92610db`, `8335993`

- 자동매매 프로젝트의 기본 방향을 README에 정리함.
- 거시경제 지표, 주가 데이터, 기술적 지표를 결합해 매수 타점을 찾는 구조로 프로젝트를 설명함.
- 초기에는 아이디어와 역할 분담 중심으로 문서화함.
- 포트폴리오 관점에서는 아직 구현보다 컨셉 정리에 가까운 단계였음.

## v0.2.0 - 데이터 분석과 2단계 피처 엔지니어링 시작 (2026-02-27 ~ 2026-02-28)

커밋: `f562c82`, `bfa6ba2`

- 본격적으로 Binance/시세 데이터 분석을 진행함.
- `quant.ipynb`에서 데이터 탐색과 2단계 피처 엔지니어링 흐름을 구성함.
- 단순 가격만 보는 방식에서 벗어나 거래량, 수익률, 변동성 기반 피처를 넓혀 가는 방향으로 잡음.
- 이후 실시간 모델의 feature set을 설계하는 기초가 된 작업임.

## v0.3.0 - 모델링 노트북 확장 (2026-03-02 ~ 2026-03-29)

커밋: `ef840b7`, `f6e799f`, `78d6495`

- `Modeling.ipynb`, `Long-term_model.ipynb`, `modeling_3.ipynb`를 추가함.
- 단기 예측뿐 아니라 장기 관점 모델도 실험함.
- 여러 노트북으로 아이디어를 빠르게 검증하며 모델 후보를 늘려 감.
- 이 단계에서는 실서비스 코드보다는 실험과 방향성 확인에 더 집중함.

## v0.4.0 - 포트폴리오용 README 정리 (2026-06-18)

커밋: `d11d8b1`

- README를 포트폴리오에서 바로 읽기 좋게 다시 정리함.
- 데이터 파이프라인, 실시간 피처, 멀티 호라이즌 모델링, 리스크 가드, Streamlit 대시보드를 한 흐름으로 설명함.
- 모델 성능 표와 현재 진행 상황을 추가함.
- 기존에 흩어져 있던 설명을 프로젝트 스냅샷 형태로 묶어서 첫인상이 좋아지도록 다듬음.

## v0.5.0 - 모의투자 서버와 confidence 기반 갈아타기 로직 추가 (2026-06-21)

커밋: `503d4b6`

- `by_kang/EDITED_BY_LOKI` 아래에 Flask API, paper trading engine, Telegram 알림, risk manager, Docker/배포 문서를 추가함.
- 기존에는 한 번 매수하면 익절, 손절, 최대 보유시간 전까지 잘 팔지 않는 구조였음.
- 보유 중에도 더 좋은 confidence 신호가 나오면 기존 포지션을 팔고 더 강한 후보로 갈아타는 로직을 추가함.
- 신규 매수 시 confidence를 `high`, `medium` 같은 라벨이 아니라 실제 모델 확률로 저장하도록 바꿈.
- `SWITCH_MIN_CONFIDENCE_DELTA` 설정을 추가해서 갈아타기 민감도를 조절할 수 있게 함.
- 실제 `.env`, 모델 pkl, venv, 키 파일은 GitHub에 올리지 않도록 `.gitignore`를 추가함.

## v0.6.0 - 인간 행동 패턴 기반 실시간 피처와 재학습 반영 (2026-06-23)

커밋: `ca2d86e`

- 인간 투자자의 FOMO, 패닉셀, 라운드 피겨 집착, 유동성 사냥, 시간대별 활동성, 비트코인 이후 알트 순환매 패턴을 실시간 계산 가능한 피처로 반영함.
- 피처 수가 `85개`에서 `104개`로 늘어남.
- GPU로 재학습하고, 저장된 모델은 CPU 실시간 추론이 가능하도록 설정함.
- 후보 모델은 depth 2~3 XGBoost로 제한해 추론 지연을 가볍게 유지함.
- 새 모델 기준 20종목 추론 지연은 대략 2.7~3.1ms 수준으로 확인함.
- `short_30m`은 비용 차감 평균 수익률이 양수로 유지됨.
- `long_2d`는 net이 가장 좋지만 trade coverage가 높아서 리스크 가드와 포지션 사이징이 반드시 필요함.
- `long_30d`, `long_60d`는 최신 테스트 구간에서 계속 부진해서 실거래 후보로 쓰지 않는 게 맞다고 판단함.
- funding rate와 open interest는 현재 parquet에 히스토리가 없어서 중립값으로 들어감. 나중에 파생상품 히스토리를 붙이면 같은 피처명으로 재학습 가능하게 열어둠.
- 모델 바이너리와 실제 API 값은 GitHub에 올리지 않고, 메트릭 JSON과 리포트만 업로드함.

## 현재 상태 요약

- 프로젝트는 아이디어 문서화 단계에서 데이터 분석, 모델 실험, 실시간 추론, 모의투자 서버, 리스크 관리, 재학습까지 확장된 상태임.
- 단기 실시간 모델은 CPU 추론 가능성을 확인함.
- 장기 모델은 아직 실거래 적용보다 연구/모니터링 용도로 두는 편이 안전함.
- 실제 API 토큰은 로컬 `.env`에만 있고 GitHub에는 마스킹된 샘플과 ignore 규칙만 올라가 있음.
- 다음 단계는 funding rate, open interest, order book depth 같은 파생/호가 데이터를 히스토리로 붙여서 시장 과열도 피처를 실제 학습에 반영하는 것임.
